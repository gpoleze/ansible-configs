---
- hosts: homeserver
  remote_user: homeserver
  become: true
  
  roles:
  - create_ansible_user
  
  handlers:
  - name: restart ssh
    ansible.builtin.service:
      name: sshd
      state: restarted

  vars_files:
  - vars/homeserver.vault.yml